# Status — atualizado 2026-08-09

## Onde estamos

**Fase: implementação concluída (branch `implementacao`). 14 de 14 tasks do plano feitas + revisão final de branch.**

- [x] Brainstorming e design aprovados pelo Rafael
- [x] Spec escrito, com o conteúdo integral do folder transcrito no apêndice
- [x] Plano de implementação em 14 tarefas TDD
- [x] Brain Obsidian criado
- [x] Repositório publicado no GitHub
- [x] Task 1 — scaffold Next.js + Tailwind + Vitest
- [x] Task 2 — schema Zod, `content/site.json` (todo o texto do folder, 20 declarações de credo em 6 grupos) e `content/load.ts` (carregador validado)
- [x] Task 3 — `lib/tema.ts` (`variaveisDeTema`), tokens ligados ao Tailwind via `@theme inline` em `app/globals.css`, `<body>` com fundo verde escuro / texto creme, `app/layout.tsx` reescrito com fonte Poppins (`next/font/google`) e metadata a partir de `content/load.ts`
- [x] Task 4 — primitivas decorativas: `components/decor/Blob.tsx`, `components/decor/Circulo.tsx`, `components/ui/SeparadorXXX.tsx`, todas `aria-hidden="true"`/`focusable="false"`
- [x] Task 5 — primitivas de interface: `components/ui/Trilha.tsx`, `components/ui/Capitulo.tsx`, `components/ui/Chip.tsx`, `components/ui/Acordeao.tsx` (único client component até agora). Achados de revisão corrigidos: offset da linha da Trilha alinhado ao centro do nó (`1.875rem`/`md:2.875rem`) e painel do Acordeão passou a ficar sempre montado no DOM, escondido via `hidden` nativo (não desmontado) — ver [[log/decisions]] 2026-08-08.
- [x] Task 6 — topo da página: `components/sections/Hero.tsx` (`h1`, subtítulo, CTA de âncora, `Blob` decorativo) e `components/sections/AcoesRapidas.tsx` (nav com um link por ação — Como chegar, Domingos 18h, Instagram, YouTube — ícones SVG inline). Achados de revisão corrigidos: teste travando a regra de contraste do subtítulo (creme no mobile / limão só a partir do md:) e regra de link externo trocada de `startsWith('http')` para regex `^https?://`, com `content/schema.ts` validando o formato de `href` de `acoesRapidas` — ver [[log/decisions]] 2026-08-08.
- [x] Task 7 — capítulos `components/sections/Historia.tsx` e `components/sections/Lideranca.tsx`, ambos só conteúdo (texto em creme) dentro de `Capitulo` (Task 5), que já fornece `<section id>`, nó da trilha, rótulo e `<h2>` — ver [[log/decisions]] 2026-08-08. Achado de revisão corrigido: teste da `Historia` que contava `section#historia p` (acoplado à estrutura interna do `Capitulo`) trocado por asserção de conteúdo — ver [[log/decisions]] 2026-08-08.
- [x] Task 8 — capítulo `components/sections/Proposito.tsx` (missão em destaque, visão e valores como `<ul>`, valores com `Chip`), dentro de `Capitulo` (Task 5). `<h3>` de Visão e Valores em creme, nunca limão — ver [[log/decisions]] 2026-08-08. Achados de revisão corrigidos: teste travando ordem/conteúdo dos itens (não só contagem), visão trocada de `<ol>` para `<ul>` (ordem não confirmada como significativa — pendente checar com a liderança da igreja) e `key={i}` no lugar de `key={item}` — ver [[log/decisions]] 2026-08-08.
- [x] Task 9 — capítulo `components/sections/Cuidado.tsx` (intro + três pilares Conectar/Crescer/Servir, cada um em `<h3>` dentro de um cartão com borda), dentro de `Capitulo` (Task 5). Único capítulo até agora cujo schema tem campo opcionalmente vazio por item de lista (`pilar.subtitulo`): a regra de degradação do projeto (campo vazio não renderiza elemento vazio) foi aplicada com `{pilar.subtitulo && (...)}`, e o `<p>` resultante carrega `data-subtitulo` para o teste contar subtítulos sem depender do texto — ver [[log/decisions]] 2026-08-08. Achados de revisão corrigidos: teste travando o texto de cada pilar dentro do bloco do seu próprio `<h3>` (não bastava checar presença dos três textos em qualquer lugar) e subtítulo trocado de `text-creme/70` (reprova AA, 4,497:1) para `text-creme/90` (6,23:1) — ver [[log/decisions]] 2026-08-08.
- [x] Task 10 — capítulo `components/sections/Credo.tsx`, as 20 declarações doutrinárias (`site.credo.grupos`, 6 grupos) em acordeão (`Acordeao`, Task 5), dentro de `Capitulo` (Task 5). Cada grupo vira uma `<div id="credo-<id>">` envolvendo um `Acordeao` de um único item — a âncora `#credo-<id>` é responsabilidade do `Credo`, não do `Acordeao`, para manter o acordeão genérico. `<h4>` de cada declaração é creme (não limão: em `text-sm` o limão reprovaria AA) — ver [[log/decisions]] 2026-08-08. Achados de revisão corrigidos: teste travando a correspondência entre o título de cada declaração e o seu próprio texto (as 20 declarações do fixture, não só uma), `key={i}` no lugar de `key={declaracao.titulo}`, e `content/schema.ts` passou a rejeitar `id` de grupo de credo duplicado (mensagem em português voltada para quem edita o JSON, não para programador) — ver [[log/decisions]] 2026-08-08.
- [x] Task 11 — `components/sections/Citacao.tsx` (citação fora da trilha, limão em `text-2xl`/`md:text-4xl` — o único lugar da página onde limão aparece em texto, porque só a partir de 24px o contraste de 4,02:1 do limão passa no AA), `components/sections/Visita.tsx` (último capítulo, dentro de `Capitulo` — cartão creme com texto grafite, invertendo o fundo escuro da página para marcar o fim da trilha) e `components/sections/Rodape.tsx` (fora da trilha, links de Instagram/YouTube em nova aba). Regra de degradação aplicada em dois pontos da `Visita`: `contato.telefone === ''` não renderiza o botão de telefone, `contato.mapaEmbedUrl === ''` não renderiza o iframe (sobra só o link "Abrir no Google Maps") — os dois casos batem com o conteúdo real de `site.json` (telefone e demais campos de contato ainda vazios). Implementado verbatim a partir de `.superpowers/sdd/task-11-brief.md` — ver [[log/decisions]] 2026-08-08.
- [x] Correção de revisão da Task 11 — os cinco rótulos hardcoded ("Abrir no Google Maps", "Telefone", `title="Mapa"`, "Instagram", "YouTube") movidos para `content/site.json` (`visita.ctaMapsTexto`/`ctaTelefoneTexto`/`mapaTitulo`, `redes.instagramRotulo`/`youtubeRotulo`) com campos correspondentes em `content/schema.ts`; teste de horários travando rótulo↔valor com fixture de dois horários (confirmado por mutação); `key={i}` no `.map` de horários; iframe do mapa trocado de `referrerPolicy="no-referrer-when-downgrade"` para `"no-referrer"` (escolha deliberada de privacidade); `text-creme/80` do rodapé trocado para `/90`, alinhando ao piso fixado nas Tasks 9–10 — ver [[log/decisions]] 2026-08-08.
- [x] Task 12 — `app/page.tsx` montado: `Hero`/`AcoesRapidas` fora da trilha, `Trilha` envolvendo os seis capítulos (`Historia`/`Proposito`/`Cuidado`/`Lideranca`/`Credo`/`Visita`), `Citacao`/`Rodape` fora da trilha. **A página existe de verdade pela primeira vez** — antes disso `app/page.tsx` era o placeholder do `create-next-app` e nenhuma seção era consumida por nada. Verificação visual real no navegador (não só aritmética): alinhamento da linha da trilha com o centro dos nós confirmado por geometria exata em 375px e 1280px (0,5px de diferença, subpixel); as quatro ações rápidas cabem sem rolagem no celular; acordeão do credo abre/fecha (capturado em tela); cartão creme da Visita se destaca; mapa do Google carrega (capturado em tela + confirmado por `performance` API). Ressalva registrada: a ferramenta de captura de tela desta sessão não produz imagem em 1280px além de ~600–1000px de rolagem (fica em branco) — comportamento e CSS computados conferidos por DOM/JS nesse caso, não por pixel. Achado de conteúdo (não de código): "Generosidade" colide como texto entre um valor do Propósito (Task 8) e um título de declaração do Credo (Task 10) — o teste da task foi escopado à seção do Propósito para não depender de unicidade textual que o conteúdo real não garante — ver [[log/decisions]] 2026-08-08.
- [x] Task 13 — camada de SEO: `lib/jsonld.ts` (`dadosDaIgreja`, JSON-LD tipo `Church`), `components/seo/JsonLd.tsx` (injeta o `<script type="application/ld+json">`), `app/layout.tsx` com `metadata` completo (title com template, canonical, Open Graph `locale: pt_BR`, Twitter Card), `app/sitemap.ts` e `app/robots.ts` (bloqueia `/editar` e `/api/`, cinto e suspensório mesmo essas rotas ainda não existindo — vêm na Task 14 e já dariam 404). `dadosDaIgreja` omite `telephone`/`email`/`geo` quando vazios — bate com `site.json` real, onde esses campos ainda são string vazia. Verificado com `curl` contra o dev server (porta 4321, fixada em `.claude/launch.json` porque 3000 e 8787 estavam ocupadas nesta máquina): o JSON-LD sai correto no HTML, `/robots.txt` e `/sitemap.xml` respondem — ver [[log/decisions]] 2026-08-09. Achados de revisão corrigidos: `JsonLd.tsx` passou a escapar `"<"` na serialização (`.replace(/</g, '\\u003c')`) para não deixar um campo de texto livre com `"</script>"` fechar a tag antes da hora, e `diaSemana` em `content/schema.ts` trocou de `z.string()` livre para `z.enum` dos sete dias em inglês, com mensagem em português — ambos importam porque a Task 14 entrega texto livre digitado por alguém da igreja nesses mesmos campos — ver [[log/decisions]] 2026-08-09.
- [x] Task 14 — `app/api/content/route.ts` (`GET`/`PUT` de `content/site.json`, validado por `siteSchema`, 404 em produção) e `app/editar/page.tsx` + `components/editor/FormularioConteudo.tsx` (formulário que achata o JSON inteiro em campos por caminho, ex.: `visita.titulo`, `credo.grupos.0.nome`, um `<textarea>` por campo). Implementado verbatim a partir de `.superpowers/sdd/task-14-brief.md`, com um ajuste de ambiente no teste: no Node 26 desta máquina, `Object.defineProperty(process.env, 'NODE_ENV', { value, configurable: true })` (como o brief prescreve) lança `TypeError` porque o `process.env` do Node passou a exigir `writable`/`enumerable` explícitos no descriptor — corrigido acrescentando as duas propriedades, sem mudar o comportamento testado. `content/schema.ts` ganhou `z.config(pt())` (locale do Zod) para que mensagens de erro sem customização própria (a maioria dos campos — só `href`, `diaSemana` e `id` de credo duplicado tinham mensagem pt-BR própria) também saiam em português em vez do inglês técnico padrão (`"Too small: expected string to have >=1 characters"` virou `"Muito pequeno: esperado que string tivesse >=1 caracteres"`) — `zod` só é importado nesse arquivo no repo inteiro, sem efeito colateral em outro schema. **Verificação manual real, não simulada**: editei `visita.texto` pelo formulário no navegador (Browser pane, clique real no botão Salvar), confirmei que `content/site.json` mudou em disco e que `/` passou a mostrar o texto novo sem restart do dev server, depois desfiz a edição e confirmei com `git diff -- content/site.json` (zero linhas) e md5 idêntico ao original. Rodei `npm run build && PORT=4321 npm start` e confirmei com `curl` real contra o servidor de produção: `GET /editar` → 404, `GET /api/content` → 404, `PUT /api/content` → 404, `GET /` → 200 (site continua servindo). Achado registrado sem correção (não é bug, é risco documentado): o achatamento do formulário converte todo valor em string ao salvar; hoje **todo** valor-folha de `site.json` já é string (confirmado programaticamente, nenhum `z.number()`/`z.boolean()` no schema), então é inofensivo — mas se um campo numérico/booleano for adicionado no futuro, o formulário vai continuar oferecendo `<textarea>` para ele e o `PUT` vai passar a rejeitar o payload inteiro com 422 (Zod não faz coerção de tipo), travando a edição de todos os campos até o componente ganhar um input dedicado para esse campo. Relatório completo em `.superpowers/sdd/task-14-report.md` (não versionado — `.superpowers/` é gitignored) — ver [[log/decisions]] 2026-08-09.
- [x] Correção de revisão da Task 14 — 5 achados (4 Important, 1 Minor) na trava de `/editar`/`/api/content`, a checagem mais crítica do projeto. Trava invertida de `=== 'production'` (fail-open: qualquer valor não exatamente `'production'` — indefinido, `'staging'`, typo — deixava a rota aberta) para `=== 'development'` (allowlist, fail-closed), extraída para módulo único `lib/ambiente.ts` (`ehDesenvolvimento()`) consumido por `app/api/content/route.ts` e `app/editar/page.tsx`, que antes reimplementavam a checagem cada um por conta própria. `tests/editor/rota.test.ts` ganhou casos para `NODE_ENV` indefinido/`'test'`/`'staging'` (antes só os dois extremos eram testados) e cobertura nova de `app/editar/page.tsx` (não tinha teste). Confirmado que os testes pegam o problema de verdade: revertida a trava, 9 de 15 testes falharam nos casos do meio, restaurada a correção, 15/15 voltaram a passar. `PUT` passou a gravar `content/site.json` de forma atômica (escreve em temporário + `rename`, em vez de `writeFile` direto — evita arquivo truncado se o processo morrer no meio). `GET` passou a validar o que lê do disco com `siteSchema` antes de devolver (antes devolvia `JSON.parse` cru). Verificação de produção repetida depois das correções: `npm run build && PORT=4321 npm start`, `curl` confirma 404 em `/editar`/`/api/content` (GET e PUT) e 200 em `/`; servidor derrubado, porta livre. `npm test` (83 testes), `npm run lint`, `npx tsc --noEmit`, `npm run build` passam — ver [[log/decisions]] 2026-08-09.
- [x] Revisão final de branch (4 grupos, antes do lançamento) — ver [[log/decisions]] 2026-08-09 e `.superpowers/sdd/revisao-final-report.md` (não versionado) para o relatório completo:
  1. **Armadilhas latentes do editor**: `Visita.tsx` — `visita.texto` e o endereço eram dois `<p>` sem margem entre eles (preflight do Tailwind zera margem de parágrafo); invisível hoje porque `visita.texto` é vazio, mas colaria no endereço no dia em que o Rafael preenchesse o campo pelo `/editar` — corrigido com `mt-3`, teste novo cobre com o campo preenchido. `Hero.tsx` — o blob decorativo atrás do subtítulo (`text-verde-limao/25`) tingia o fundo para um tom que reduz o contraste com o creme; a distância entre título (duas linhas) e subtítulo escondia isso por acidente, e encurtar `hero.titulo` pelo `/editar` empurraria o subtítulo pra dentro do blob — opacidade reduzida para `/15` (medido no navegador real via `getComputedStyle` + composição em `<canvas>`, validado contra um ponto de referência exato: contraste subiu de 4,89:1 para 5,72:1 contra creme; o relatório completo documenta uma divergência honesta com o número hipotético do enunciado da tarefa, 3,97:1/`#717F4E`, que minha medição direta não reproduziu).
  2. **Identidade e documentação**: `app/favicon.ico` (triângulo da Vercel) trocado por `app/icon.svg` — placeholder explícito (folha/árvore estilizada, limão sobre verde escuro; a igreja tem um emblema circular real, não enviado — pendência na tabela abaixo). `README.md` reescrito para o estado real do projeto (era "planejamento concluído, implementação não iniciada"); `CLAUDE.md` corrigido de "Next.js 15" para 16.
  3. **Consistência e limpeza**: anel de foco unificado em `app/globals.css` (`@utility anel-de-foco`/`anel-de-foco-escuro`, Tailwind v4), aplicado também aos dois links do rodapé (não tinham nada — já foi esquecido uma vez); `key={acao.rotulo}` da `AcoesRapidas` trocado por `key={i}`; `components/decor/Circulo.tsx`, `components/ui/SeparadorXXX.tsx` e os cinco SVGs do scaffold em `public/` removidos (código morto, só testados pelos próprios testes).
  4. **Buracos de teste**: `tests/components/credo.test.tsx` ganhou teste travando que dois grupos do Credo podem ficar abertos ao mesmo tempo na página real (cada grupo é seu próprio `Acordeao`) — contrato diferente do `Acordeao` isolado, mantido à parte. `tests/seo/metadata.test.tsx` novo cobre `app/layout.tsx`/`app/robots.ts`/`app/sitemap.ts`, que não tinham nenhum teste (precisou de mock de `next/font/google` em `vitest.setup.ts`, só função dentro do pipeline de build do Next). `tests/pagina.test.tsx` ganhou hierarquia de cabeçalhos sem salto de nível e confirmação de que `<footer>` não é descendente de `<main>` (bug real já corrigido, agora com rede de teste). `tests/smoke.test.tsx` (componente inventado, não pagava aluguel) removido.

  Suíte final: 97 testes (15 arquivos), `npm run build`, `npm run lint` e `npx tsc --noEmit` verdes. Verificação de produção repetida: `npm run build && PORT=4321 npm start`, `curl` confirma 404 em `/editar` e `/api/content`, 200 em `/`, `/icon.svg` servindo como rota estática; servidor derrubado, porta 4321 confirmada livre.

## Próximo passo

Implementação completa. Falta só o que depende do Rafael/igreja, listado em "Bloqueios reais" abaixo — nenhum é código. Antes de publicar, seguir a checklist em [[projeto/deploy]] (os itens 1–3, testes/build verdes e 404 de `/editar`/`/api/content` em produção, já foram verificados nesta sessão, na Task 14).

## Bloqueios reais

Nenhum bloqueia a implementação. Os itens abaixo bloqueiam apenas o **lançamento**, e todos dependem do Rafael ou da igreja, não de código:

| Item | Situação |
|---|---|
| Domínio | Não registrado. Sugestão: `arvoredavidalrv.com.br` no Registro.br, ~R$40/ano. Desde a Task 13, esse domínio inexistente já sai no HTML publicado — `metadataBase`/canonical, `og:url` e o `url` do JSON-LD (`lib/jsonld.ts`) apontam todos para `https://arvoredavidalrv.com.br`. Não quebra o build, mas até o domínio ser registrado e apontado, links compartilhados e o JSON-LD levam a um endereço que não resolve |
| Hospedagem | Vercel, plano gratuito — conta ainda não criada |
| WhatsApp / telefone | Desconhecido. Fica como campo vazio; o botão simplesmente não renderiza |
| E-mail, CEP, coordenadas | Desconhecidos, mesmo tratamento |
| Horários de GCs | Desconhecidos. Só o culto de domingo 18h está confirmado |
| Fotos | Nenhum arquivo recebido. Os slots existem e renderizam gradientes |
| Imagem Open Graph | Precisa de arte 1200×630. Sem ela, o card do link fica sem imagem |
| Perfil da Empresa no Google | Não verificado se existe. Ver [[projeto/seo]] — importa mais que o site |
| Logo real | `app/icon.svg` hoje é um placeholder (folha/árvore estilizada, limão sobre verde escuro) — a igreja tem um emblema circular próprio que não foi enviado. Trocar o arquivo quando o Rafael mandar o logo real (SVG de preferência) |

## Dados confirmados

Endereço, horário e redes vieram do perfil do Instagram em 2026-08-08. Ver [[projeto/objetivo]].
